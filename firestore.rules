rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /user_profiles/{userId} {
      // Allow reading if authenticated or if matching user document
      allow read: if true;
      // Allow writing to user profile
      allow write: if true;
    }
  }
}
